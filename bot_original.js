// Complete bot code goes here. Make sure to include all necessary imports, variable declarations, and the main functionality for the bot. Ensure that the code initializes the panel to open directly on load and remove the initial setTimeout that shows "ACTIVO".

// Example structure of the bot code:

const fs = require('fs');
const { Panel } = require('some-panel-library');

const panel = new Panel();

// Open the panel directly on load
panel.open();

// Add remaining bot functionality here
...
